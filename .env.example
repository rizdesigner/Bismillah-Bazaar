# PostgreSQL connection string used by Prisma.
# Local dev default (created during setup). Replace with your
# hosted database URL (Vercel Postgres / Neon / Supabase) in production.
DATABASE_URL="postgresql://bismillah_app:bismillah_dev_password@localhost:5432/bismillah_bazaar?schema=public"
